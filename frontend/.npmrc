# Algumas libs ainda declaram peer dependency de React 16/17
# (react-color, react-modal-image, use-sound, emoji-mart, etc.).
# Elas funcionam com React 18; este flag evita falha de ERESOLVE no install.
# Remover quando essas dependências forem atualizadas.
legacy-peer-deps=true
